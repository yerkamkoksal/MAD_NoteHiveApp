<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo4"
        android:label="@string/app_name"
        android:permission=""
        android:roundIcon="@mipmap/logo4"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteHiveProjectTry"
        tools:targetApi="31" >
        <activity
            android:name=".HomePageActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ViewActivitiesActivity"
            android:exported="false"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".AddEventActivity"
            android:exported="false"
            android:parentActivityName=".HomePageActivity" />
        <activity
            android:name=".ActivityDescriptionActivity"
            android:exported="false"
            android:parentActivityName=".ViewActivitiesActivity" />
    </application>

</manifest>